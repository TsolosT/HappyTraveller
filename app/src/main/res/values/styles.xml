<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryWhite</item>
        <item name="colorAccent">@color/colorBlack</item>
        <item name="android:textColorPrimary">@color/colorPrimaryWhite</item>
        <item name="android:textColorSecondary">@color/colorPrimaryWhite</item>
        <item name="android:navigationBarColor">@color/common_google_signin_btn_text_light_default</item>
        <item name="android:colorForeground">@color/colorPrimaryWhite</item>
        <item name="android:colorBackground">@color/background_material_light</item>
        <item name="android:windowBackground">@color/colorPrimaryWhite</item>
        <item name="android:statusBarColor">@color/common_google_signin_btn_text_light_default
        </item>
    </style>

    <style name="Dark" parent="AppTheme" />

</resources>
